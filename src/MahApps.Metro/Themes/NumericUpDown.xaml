<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:mah="clr-namespace:MahApps.Metro.Controls"
                    xmlns:mahConverters="clr-namespace:MahApps.Metro.Converters">

    <Thickness x:Key="NumericUpDownBorderThickness">1,1,1,1</Thickness>
    <Thickness x:Key="NumericUpDownSelectedBorderThickness">1,1,1,1</Thickness>
    <Thickness x:Key="NumericUpDownSpinButtonRightBorderThickness">0,0,0,0</Thickness>
    <Thickness x:Key="NumericUpDownSpinButtonLeftBorderThickness">0,0,0,0</Thickness>
    <Thickness x:Key="NumericUpDownBottomEdgeThickness">0,0,0,0</Thickness>

    <Style x:Key="MahApps.Styles.Button.NumericUpDown.Spin" TargetType="{x:Type ButtonBase}">
        <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Transparent}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.Text}" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ButtonBase}">
                    <Border x:Name="Border"
                            Margin="0"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{TemplateBinding mah:ControlsHelper.CornerRadius}"
                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                        <Grid x:Name="ContentGrid">
                            <Grid.Clip>
                                <!--  a clip lives in the coordinates of the element it sits on, so it takes the size of this grid and the corners the border leaves over  -->
                                <MultiBinding Converter="{x:Static mahConverters:ClipGeometryConverter.Instance}">
                                    <Binding ElementName="ContentGrid" Path="ActualWidth" />
                                    <Binding ElementName="ContentGrid" Path="ActualHeight" />
                                    <Binding ElementName="Border" Path="CornerRadius" />
                                    <Binding ElementName="Border" Path="BorderThickness" />
                                    <Binding ElementName="Border" Path="Padding" />
                                </MultiBinding>
                            </Grid.Clip>
                            <mah:ContentControlEx x:Name="PART_ContentPresenter"
                                                  Padding="{TemplateBinding Padding}"
                                                  HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                                  Content="{TemplateBinding Content}"
                                                  ContentCharacterCasing="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(mah:ControlsHelper.ContentCharacterCasing)}"
                                                  ContentStringFormat="{TemplateBinding ContentStringFormat}"
                                                  ContentTemplate="{TemplateBinding ContentTemplate}"
                                                  ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
                                                  RecognizesAccessKey="{TemplateBinding mah:ControlsHelper.RecognizesAccessKey}"
                                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="PART_ContentPresenter" Property="Opacity" Value="1" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="False">
                            <Setter TargetName="PART_ContentPresenter" Property="Opacity" Value="0.5" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.SystemControlForegroundChromeDisabledLow}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Gray8}" />
                <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.Accent}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.ThemeForeground}" />
                <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.ThemeBackground}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="MahApps.Styles.NumericUpDown" TargetType="{x:Type mah:NumericUpDownBase}">
        <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Control.Background}" />
        <Setter Property="BorderBrush" Value="{DynamicResource MahApps.Brushes.TextBox.Border}" />
        <Setter Property="BorderThickness" Value="{DynamicResource NumericUpDownBorderThickness}" />
        <Setter Property="ButtonDownContentTemplate">
            <Setter.Value>
                <DataTemplate>
                    <mah:PathIcon Width="14"
                                  Height="14"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"
                                  Data="M19,13H5V11H19V13Z" />
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="ButtonUpContentTemplate">
            <Setter.Value>
                <DataTemplate>
                    <mah:PathIcon Width="14"
                                  Height="14"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"
                                  Data="M19,13H13V19H11V13H5V11H11V5H13V11H19V13Z" />
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="ContextMenu" Value="{DynamicResource MahApps.TextBox.ContextMenu}" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="FontFamily" Value="{DynamicResource MahApps.Fonts.Family.Control}" />
        <Setter Property="FontSize" Value="{DynamicResource MahApps.Font.Size.Content}" />
        <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.Text}" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="MinHeight" Value="26" />
        <Setter Property="MinWidth" Value="62" />
        <Setter Property="Padding" Value="4" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Hidden" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Hidden" />
        <Setter Property="SnapsToDevicePixels" Value="true" />
        <Setter Property="SpinButtonStyle" Value="{DynamicResource MahApps.Styles.Button.NumericUpDown.Spin}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type mah:NumericUpDownBase}">
                    <!--  whole device pixels for everything inside the frame, the way Controls.TextBox.xaml does it  -->
                    <Grid SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" UseLayoutRounding="True">
                        <Border x:Name="Base"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{TemplateBinding mah:ControlsHelper.CornerRadius}"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                            <Grid x:Name="PART_InnerGrid">
                                <Grid.Clip>
                                    <!--  a clip lives in the coordinates of the element it sits on, so it takes the size of this grid and the corners the border leaves over  -->
                                    <MultiBinding Converter="{x:Static mahConverters:ClipGeometryConverter.Instance}">
                                        <Binding ElementName="PART_InnerGrid" Path="ActualWidth" />
                                        <Binding ElementName="PART_InnerGrid" Path="ActualHeight" />
                                        <Binding ElementName="Base" Path="CornerRadius" />
                                        <Binding ElementName="Base" Path="BorderThickness" />
                                        <Binding ElementName="Base" Path="Padding" />
                                    </MultiBinding>
                                </Grid.Clip>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition x:Name="PART_LeftColumn" Width="*" />
                                    <ColumnDefinition x:Name="PART_MiddleColumn" Width="Auto" />
                                    <ColumnDefinition x:Name="PART_RightColumn" Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <TextBox x:Name="PART_TextBox"
                                         Grid.Column="0"
                                         MinWidth="20"
                                         MinHeight="0"
                                         Margin="0"
                                         Padding="{TemplateBinding Padding}"
                                         HorizontalAlignment="Stretch"
                                         HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                         VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                         mah:ControlsHelper.DisabledVisualElementVisibility="Collapsed"
                                         mah:TextBoxHelper.AutoWatermark="{TemplateBinding mah:TextBoxHelper.AutoWatermark}"
                                         mah:TextBoxHelper.ButtonCommand="{TemplateBinding mah:TextBoxHelper.ButtonCommand}"
                                         mah:TextBoxHelper.ButtonCommandParameter="{TemplateBinding mah:TextBoxHelper.ButtonCommandParameter}"
                                         mah:TextBoxHelper.ButtonCommandTarget="{TemplateBinding mah:TextBoxHelper.ButtonCommandTarget}"
                                         mah:TextBoxHelper.ButtonContent="{TemplateBinding mah:TextBoxHelper.ButtonContent}"
                                         mah:TextBoxHelper.ButtonContentTemplate="{TemplateBinding mah:TextBoxHelper.ButtonContentTemplate}"
                                         mah:TextBoxHelper.ButtonFontFamily="{TemplateBinding mah:TextBoxHelper.ButtonFontFamily}"
                                         mah:TextBoxHelper.ButtonFontSize="{TemplateBinding mah:TextBoxHelper.ButtonFontSize}"
                                         mah:TextBoxHelper.ButtonTemplate="{TemplateBinding mah:TextBoxHelper.ButtonTemplate}"
                                         mah:TextBoxHelper.ButtonWidth="{TemplateBinding mah:TextBoxHelper.ButtonWidth}"
                                         mah:TextBoxHelper.ButtonsAlignment="{TemplateBinding ButtonsAlignment}"
                                         mah:TextBoxHelper.ClearTextButton="{TemplateBinding mah:TextBoxHelper.ClearTextButton}"
                                         mah:TextBoxHelper.HasText="{TemplateBinding mah:TextBoxHelper.HasText}"
                                         mah:TextBoxHelper.SelectAllOnFocus="{TemplateBinding mah:TextBoxHelper.SelectAllOnFocus}"
                                         mah:TextBoxHelper.UseFloatingWatermark="{TemplateBinding mah:TextBoxHelper.UseFloatingWatermark}"
                                         mah:TextBoxHelper.Watermark="{TemplateBinding mah:TextBoxHelper.Watermark}"
                                         mah:TextBoxHelper.WatermarkAlignment="{TemplateBinding mah:TextBoxHelper.WatermarkAlignment}"
                                         mah:TextBoxHelper.WatermarkTrimming="{TemplateBinding mah:TextBoxHelper.WatermarkTrimming}"
                                         mah:TextBoxHelper.WatermarkWrapping="{TemplateBinding mah:TextBoxHelper.WatermarkWrapping}"
                                         Background="{x:Null}"
                                         BorderThickness="0"
                                         ContextMenu="{TemplateBinding ContextMenu}"
                                         FocusVisualStyle="{x:Null}"
                                         Focusable="{TemplateBinding Focusable}"
                                         FontFamily="{TemplateBinding FontFamily}"
                                         FontSize="{TemplateBinding FontSize}"
                                         Foreground="{TemplateBinding Foreground}"
                                         HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
                                         IsReadOnly="{TemplateBinding IsReadOnly}"
                                         IsTabStop="{TemplateBinding IsTabStop}"
                                         SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                         TabIndex="{TemplateBinding TabIndex}"
                                         VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}">
                                    <TextBox.Resources>
                                        <!--
                                            The frame belongs to the control around this box, so the caret must not hand
                                            it one of its own, and neither must the edge a WinUI box draws along its
                                            bottom: that box ends where the number ends and the buttons begin, so its
                                            edge would stop there while the one below it runs the whole width.
                                        -->
                                        <Thickness x:Key="TextControlBorderThemeThicknessFocused">0</Thickness>
                                        <Thickness x:Key="TextControlBottomEdgeThemeThickness">0</Thickness>
                                    </TextBox.Resources>
                                </TextBox>

                                <RepeatButton x:Name="PART_NumericUp"
                                              Grid.Column="1"
                                              Width="{TemplateBinding UpDownButtonsWidth}"
                                              Margin="0"
                                              mah:ControlsHelper.CornerRadius="0"
                                              BorderThickness="{DynamicResource NumericUpDownSpinButtonRightBorderThickness}"
                                              Content="{TemplateBinding ButtonUpContent}"
                                              ContentStringFormat="{TemplateBinding ButtonUpContentStringFormat}"
                                              ContentTemplate="{TemplateBinding ButtonUpContentTemplate}"
                                              Delay="{TemplateBinding Delay}"
                                              Focusable="{TemplateBinding UpDownButtonsFocusable}"
                                              Style="{TemplateBinding SpinButtonStyle}" />

                                <RepeatButton x:Name="PART_NumericDown"
                                              Grid.Column="2"
                                              Width="{TemplateBinding UpDownButtonsWidth}"
                                              Margin="0"
                                              mah:ControlsHelper.CornerRadius="0"
                                              BorderThickness="{DynamicResource NumericUpDownSpinButtonRightBorderThickness}"
                                              Content="{TemplateBinding ButtonDownContent}"
                                              ContentStringFormat="{TemplateBinding ButtonDownContentStringFormat}"
                                              ContentTemplate="{TemplateBinding ButtonDownContentTemplate}"
                                              Delay="{TemplateBinding Delay}"
                                              Focusable="{TemplateBinding UpDownButtonsFocusable}"
                                              Style="{TemplateBinding SpinButtonStyle}" />
                            </Grid>
                        </Border>

                        <Border x:Name="FocusBorder"
                                Background="{x:Null}"
                                BorderBrush="{x:Null}"
                                BorderThickness="0"
                                CornerRadius="{TemplateBinding mah:ControlsHelper.CornerRadius}"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />

                        <!--
                            The edge a WinUI control draws a little stronger, and the accent while
                            the caret is in it. It is a frame of its own carrying nothing but that
                            edge, so that it follows the corners of the one behind it and is the
                            same two units on a box of any height. The sets that draw no such edge
                            leave its thickness at nought and it is not there at all.
                        -->
                        <Border x:Name="BottomEdge"
                                BorderBrush="{TemplateBinding mah:ControlsHelper.BottomBorderBrush}"
                                BorderThickness="{DynamicResource NumericUpDownBottomEdgeThickness}"
                                CornerRadius="{TemplateBinding mah:ControlsHelper.CornerRadius}"
                                IsHitTestVisible="False"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />

                        <Border x:Name="DisabledVisualElement"
                                Background="{DynamicResource MahApps.Brushes.Control.Disabled}"
                                BorderBrush="{DynamicResource MahApps.Brushes.Control.Disabled}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{TemplateBinding mah:ControlsHelper.CornerRadius}"
                                IsHitTestVisible="False"
                                Opacity="0"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                Visibility="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(mah:ControlsHelper.DisabledVisualElementVisibility), Mode=OneWay}" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="ButtonsAlignment" Value="Left" />
                                <Condition Property="SwitchUpDownButtons" Value="False" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="PART_LeftColumn" Property="Width" Value="Auto" />
                            <Setter TargetName="PART_MiddleColumn" Property="Width" Value="Auto" />
                            <Setter TargetName="PART_NumericDown" Property="BorderThickness" Value="{DynamicResource NumericUpDownSpinButtonLeftBorderThickness}" />
                            <Setter TargetName="PART_NumericDown" Property="Grid.Column" Value="1" />
                            <Setter TargetName="PART_NumericUp" Property="BorderThickness" Value="{DynamicResource NumericUpDownSpinButtonLeftBorderThickness}" />
                            <Setter TargetName="PART_NumericUp" Property="Grid.Column" Value="0" />
                            <Setter TargetName="PART_RightColumn" Property="Width" Value="*" />
                            <Setter TargetName="PART_TextBox" Property="Grid.Column" Value="2" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="ButtonsAlignment" Value="Left" />
                                <Condition Property="SwitchUpDownButtons" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="PART_LeftColumn" Property="Width" Value="Auto" />
                            <Setter TargetName="PART_MiddleColumn" Property="Width" Value="Auto" />
                            <Setter TargetName="PART_NumericDown" Property="BorderThickness" Value="{DynamicResource NumericUpDownSpinButtonLeftBorderThickness}" />
                            <Setter TargetName="PART_NumericDown" Property="Grid.Column" Value="0" />
                            <Setter TargetName="PART_NumericUp" Property="BorderThickness" Value="{DynamicResource NumericUpDownSpinButtonLeftBorderThickness}" />
                            <Setter TargetName="PART_NumericUp" Property="Grid.Column" Value="1" />
                            <Setter TargetName="PART_RightColumn" Property="Width" Value="*" />
                            <Setter TargetName="PART_TextBox" Property="Grid.Column" Value="2" />
                        </MultiTrigger>

                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="ButtonsAlignment" Value="Opposite" />
                                <Condition Property="SwitchUpDownButtons" Value="False" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="PART_LeftColumn" Property="Width" Value="Auto" />
                            <Setter TargetName="PART_MiddleColumn" Property="Width" Value="*" />
                            <Setter TargetName="PART_NumericDown" Property="BorderThickness" Value="{DynamicResource NumericUpDownSpinButtonRightBorderThickness}" />
                            <Setter TargetName="PART_NumericDown" Property="Grid.Column" Value="2" />
                            <Setter TargetName="PART_NumericUp" Property="BorderThickness" Value="{DynamicResource NumericUpDownSpinButtonLeftBorderThickness}" />
                            <Setter TargetName="PART_NumericUp" Property="Grid.Column" Value="0" />
                            <Setter TargetName="PART_RightColumn" Property="Width" Value="Auto" />
                            <Setter TargetName="PART_TextBox" Property="Grid.Column" Value="1" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="ButtonsAlignment" Value="Opposite" />
                                <Condition Property="SwitchUpDownButtons" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="PART_LeftColumn" Property="Width" Value="Auto" />
                            <Setter TargetName="PART_MiddleColumn" Property="Width" Value="*" />
                            <Setter TargetName="PART_NumericDown" Property="BorderThickness" Value="{DynamicResource NumericUpDownSpinButtonLeftBorderThickness}" />
                            <Setter TargetName="PART_NumericDown" Property="Grid.Column" Value="0" />
                            <Setter TargetName="PART_NumericUp" Property="BorderThickness" Value="{DynamicResource NumericUpDownSpinButtonRightBorderThickness}" />
                            <Setter TargetName="PART_NumericUp" Property="Grid.Column" Value="2" />
                            <Setter TargetName="PART_RightColumn" Property="Width" Value="Auto" />
                            <Setter TargetName="PART_TextBox" Property="Grid.Column" Value="1" />
                        </MultiTrigger>

                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="ButtonsAlignment" Value="Right" />
                                <Condition Property="SwitchUpDownButtons" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="PART_LeftColumn" Property="Width" Value="*" />
                            <Setter TargetName="PART_MiddleColumn" Property="Width" Value="Auto" />
                            <Setter TargetName="PART_NumericDown" Property="Grid.Column" Value="1" />
                            <Setter TargetName="PART_NumericUp" Property="Grid.Column" Value="2" />
                            <Setter TargetName="PART_RightColumn" Property="Width" Value="Auto" />
                            <Setter TargetName="PART_TextBox" Property="Grid.Column" Value="0" />
                        </MultiTrigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="DisabledVisualElement" Property="Opacity" Value="0.6" />
                        </Trigger>
                        <Trigger Property="IsReadOnly" Value="True">
                            <Setter Property="InterceptArrowKeys" Value="False" />
                            <Setter Property="InterceptManualEnter" Value="False" />
                            <Setter Property="InterceptMouseWheel" Value="False" />
                            <Setter TargetName="PART_NumericDown" Property="IsEnabled" Value="False" />
                            <Setter TargetName="PART_NumericUp" Property="IsEnabled" Value="False" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsReadOnly" Value="False" />
                                <Condition Property="InterceptManualEnter" Value="False" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="PART_TextBox" Property="IsReadOnly" Value="True" />
                        </MultiTrigger>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter TargetName="Base" Property="BorderBrush" Value="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(mah:ControlsHelper.MouseOverBorderBrush)}" />
                        </Trigger>
                        <Trigger SourceName="PART_TextBox" Property="IsSelectionActive" Value="true">
                            <Setter TargetName="BottomEdge" Property="BorderBrush" Value="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(mah:ControlsHelper.FocusBorderBrush)}" />
                            <Setter TargetName="FocusBorder" Property="BorderBrush" Value="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(mah:ControlsHelper.FocusBorderBrush)}" />
                            <Setter TargetName="FocusBorder" Property="BorderThickness" Value="{DynamicResource NumericUpDownSelectedBorderThickness}" />
                        </Trigger>
                        <Trigger Property="HideUpDownButtons" Value="True">
                            <Setter TargetName="PART_NumericDown" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="PART_NumericUp" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="TextAlignment" Value="Right" />
        <Setter Property="UpDownButtonsWidth" Value="22" />
        <Setter Property="Validation.ErrorTemplate" Value="{DynamicResource MahApps.Templates.ValidationError}" />

        <Setter Property="mah:ControlsHelper.FocusBorderBrush" Value="{DynamicResource MahApps.Brushes.TextBox.Border.Focus}" />
        <Setter Property="mah:ControlsHelper.MouseOverBorderBrush" Value="{DynamicResource MahApps.Brushes.TextBox.Border.MouseOver}" />

        <Setter Property="mah:TextBoxHelper.ButtonCommandParameter" Value="{Binding RelativeSource={RelativeSource Self}, Mode=OneWay}" />
        <Setter Property="mah:TextBoxHelper.ButtonCommandTarget" Value="{Binding RelativeSource={RelativeSource Self}, Mode=OneWay}" />
        <Setter Property="mah:TextBoxHelper.ButtonFontSize" Value="{DynamicResource MahApps.Font.Size.Button.ClearText}" />
        <Setter Property="mah:TextBoxHelper.ButtonTemplate" Value="{DynamicResource MahApps.Templates.Button.Chromeless}" />
        <Setter Property="mah:TextBoxHelper.ButtonWidth" Value="22" />
        <Setter Property="mah:TextBoxHelper.IsMonitoring" Value="True" />
        <Setter Property="mah:TextBoxHelper.WatermarkAlignment" Value="Right" />
    </Style>

    <!--
        The spin buttons of the two Windows sets. They stand apart from the Metro one rather than on
        it, because that one turns its chevron down to half while nothing is over it and fills itself
        with the accent while it is held, and neither Windows box does either of those. What answers
        the pointer here is the cell the chevron stands in, filled the way a row of a list is filled.
        Neither button needs a clip: the box gives them no corners of their own, and the grid they
        stand in is already cut to the ones the frame leaves over.
    -->
    <Style x:Key="MahApps.Styles.Button.NumericUpDown.Spin.Win10" TargetType="{x:Type ButtonBase}">
        <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.NumericUpDown.Win10.SpinButtonBackground}" />
        <Setter Property="BorderThickness" Value="0" />
        <!--  the chevron is the number beside it, and turns with it once the caret has made the box white  -->
        <Setter Property="Foreground" Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type Control}}, Path=Foreground, Mode=OneWay}" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ButtonBase}">
                    <Border x:Name="ButtonLayoutGrid"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                        <ContentPresenter x:Name="PART_ContentPresenter"
                                          Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          RecognizesAccessKey="{TemplateBinding mah:ControlsHelper.RecognizesAccessKey}"
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ButtonLayoutGrid" Property="Background" Value="{DynamicResource MahApps.Brushes.NumericUpDown.Win10.SpinButtonBackgroundPointerOver}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="ButtonLayoutGrid" Property="Background" Value="{DynamicResource MahApps.Brushes.NumericUpDown.Win10.SpinButtonBackgroundPressed}" />
                        </Trigger>
                        <!--
                            A button that can count no further is grey rather than gone: the value
                            has reached its minimum or its maximum, and a chevron that disappears
                            there leaves a gap where the reader expects a pair.
                        -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="PART_ContentPresenter" Property="TextElement.Foreground" Value="{DynamicResource MahApps.Brushes.NumericUpDown.Win10.SpinButtonForegroundDisabled}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="VerticalContentAlignment" Value="Center" />
    </Style>

    <!--
        And the WinUI one, which parts company over the chevron: there it is the quieter of the two
        text colours at rest and the full one under the pointer, the way the delete button of that
        text box is drawn, rather than the number it steps.
    -->
    <Style x:Key="MahApps.Styles.Button.NumericUpDown.Spin.WinUI"
           BasedOn="{StaticResource MahApps.Styles.Button.NumericUpDown.Spin.Win10}"
           TargetType="{x:Type ButtonBase}">
        <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.NumericUpDown.WinUI.SpinButtonBackground}" />
        <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.NumericUpDown.WinUI.SpinButtonForeground}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ButtonBase}">
                    <Border x:Name="ButtonLayoutGrid"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                        <ContentPresenter x:Name="PART_ContentPresenter"
                                          Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          RecognizesAccessKey="{TemplateBinding mah:ControlsHelper.RecognizesAccessKey}"
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ButtonLayoutGrid" Property="Background" Value="{DynamicResource MahApps.Brushes.NumericUpDown.WinUI.SpinButtonBackgroundPointerOver}" />
                            <Setter TargetName="PART_ContentPresenter" Property="TextElement.Foreground" Value="{DynamicResource MahApps.Brushes.NumericUpDown.WinUI.SpinButtonForegroundPointerOver}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="ButtonLayoutGrid" Property="Background" Value="{DynamicResource MahApps.Brushes.NumericUpDown.WinUI.SpinButtonBackgroundPressed}" />
                            <Setter TargetName="PART_ContentPresenter" Property="TextElement.Foreground" Value="{DynamicResource MahApps.Brushes.NumericUpDown.WinUI.SpinButtonForegroundPressed}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="PART_ContentPresenter" Property="TextElement.Foreground" Value="{DynamicResource MahApps.Brushes.NumericUpDown.WinUI.SpinButtonForegroundDisabled}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--
        The Windows 10 box, which is the text box of that set with a pair of buttons standing in it.
        It is that box down to the detail: the same fill, the same two units of frame, the same
        padding and the same delete button, a frame that turns into the accent and a fill that turns
        white once the caret is in. The chevrons are the ones the list of a combo box hangs behind,
        since the two controls sit next to each other in a form and UWP draws them alike. A box that
        is switched off says so with its colours rather than with the veil the Metro box draws over
        itself.
    -->
    <Style x:Key="MahApps.Styles.NumericUpDown.Win10"
           BasedOn="{StaticResource MahApps.Styles.NumericUpDown}"
           TargetType="{x:Type mah:NumericUpDownBase}">
        <Style.Resources>
            <Thickness x:Key="NumericUpDownSelectedBorderThickness">2,2,2,2</Thickness>
            <!--  the box the number is typed into is the one of this set, even where a single control wears the style alone  -->
            <Style BasedOn="{StaticResource MahApps.Styles.TextBox.Win10}" TargetType="{x:Type TextBox}" />
        </Style.Resources>
        <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.NumericUpDown.Win10.Background}" />
        <Setter Property="BorderBrush" Value="{DynamicResource MahApps.Brushes.NumericUpDown.Win10.BorderBrush}" />
        <Setter Property="BorderThickness" Value="2" />
        <Setter Property="ButtonDownContentTemplate">
            <Setter.Value>
                <DataTemplate>
                    <mah:FontIcon FontFamily="{DynamicResource MahApps.Fonts.Family.SymbolTheme}"
                                  FontSize="12"
                                  Glyph="&#xE70D;"
                                  UseLayoutRounding="False" />
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="ButtonUpContentTemplate">
            <Setter.Value>
                <DataTemplate>
                    <mah:FontIcon FontFamily="{DynamicResource MahApps.Fonts.Family.SymbolTheme}"
                                  FontSize="12"
                                  Glyph="&#xE70E;"
                                  UseLayoutRounding="False" />
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="FontSize" Value="{DynamicResource MahApps.Font.Size.Default}" />
        <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.NumericUpDown.Win10.Foreground}" />
        <Setter Property="MinHeight" Value="32" />
        <Setter Property="Padding" Value="10 6 6 5" />
        <Setter Property="SpinButtonStyle" Value="{DynamicResource MahApps.Styles.Button.NumericUpDown.Spin.Win10}" />
        <Setter Property="TextAlignment" Value="Left" />
        <Setter Property="UpDownButtonsFocusable" Value="False" />
        <Setter Property="UpDownButtonsWidth" Value="34" />
        <Setter Property="mah:ControlsHelper.DisabledBorderBrush" Value="{DynamicResource MahApps.Brushes.NumericUpDown.Win10.BorderBrushDisabled}" />
        <!--  no veil over a box that is off: this set says it with the colours, the way its text box does  -->
        <Setter Property="mah:ControlsHelper.DisabledVisualElementVisibility" Value="Collapsed" />
        <Setter Property="mah:ControlsHelper.FocusBorderBrush" Value="{DynamicResource MahApps.Brushes.NumericUpDown.Win10.BorderBrushFocused}" />
        <Setter Property="mah:ControlsHelper.MouseOverBorderBrush" Value="{DynamicResource MahApps.Brushes.NumericUpDown.Win10.BorderBrushPointerOver}" />
        <Setter Property="mah:TextBoxHelper.ButtonContent" Value="&#xE10A;" />
        <Setter Property="mah:TextBoxHelper.ButtonContentTemplate">
            <Setter.Value>
                <DataTemplate>
                    <mah:FontIcon FontFamily="{DynamicResource MahApps.Fonts.Family.SymbolTheme}"
                                  FontSize="12"
                                  Glyph="{Binding}"
                                  UseLayoutRounding="False" />
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="mah:TextBoxHelper.ButtonTemplate" Value="{DynamicResource MahApps.Templates.Button.TextControl.Win10}" />
        <Setter Property="mah:TextBoxHelper.ButtonWidth" Value="30" />
        <!--  the number stands on the left here, and what stands in for it while there is none belongs there too  -->
        <Setter Property="mah:TextBoxHelper.WatermarkAlignment" Value="Left" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.NumericUpDown.Win10.BackgroundPointerOver}" />
            </Trigger>
            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.NumericUpDown.Win10.BackgroundFocused}" />
                <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.NumericUpDown.Win10.ForegroundFocused}" />
            </Trigger>
            <!--
                A box that is switched off looks switched off, even when the caret was in it when it
                happened: of two triggers that set the same thing, the later one wins, and the focus
                stays inside a control that has just been disabled.
            -->
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.NumericUpDown.Win10.BackgroundDisabled}" />
                <Setter Property="BorderBrush" Value="{DynamicResource MahApps.Brushes.NumericUpDown.Win10.BorderBrushDisabled}" />
                <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.NumericUpDown.Win10.ForegroundDisabled}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!--
        WinUI on top of it, the way the text box has it: rounded corners and a border a touch stronger
        along the bottom edge, which turns into the accent while the caret is in. That edge is a frame
        of its own two units thick either way, so the box stays as tall as it was and nothing under it
        moves, and the frame the caret used to draw round the whole box is gone with it.
    -->
    <Style x:Key="MahApps.Styles.NumericUpDown.WinUI"
           BasedOn="{StaticResource MahApps.Styles.NumericUpDown.Win10}"
           TargetType="{x:Type mah:NumericUpDownBase}">
        <Style.Resources>
            <Thickness x:Key="NumericUpDownSelectedBorderThickness">0,0,0,0</Thickness>
            <Thickness x:Key="NumericUpDownBottomEdgeThickness">0,0,0,2</Thickness>
            <Style BasedOn="{StaticResource MahApps.Styles.TextBox.WinUI}" TargetType="{x:Type TextBox}" />
        </Style.Resources>
        <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.NumericUpDown.WinUI.Background}" />
        <Setter Property="BorderBrush" Value="{DynamicResource MahApps.Brushes.NumericUpDown.WinUI.BorderBrush}" />
        <Setter Property="BorderThickness" Value="1 1 1 2" />
        <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.NumericUpDown.WinUI.Foreground}" />
        <Setter Property="SpinButtonStyle" Value="{DynamicResource MahApps.Styles.Button.NumericUpDown.Spin.WinUI}" />
        <Setter Property="mah:ControlsHelper.BottomBorderBrush" Value="{DynamicResource MahApps.Brushes.NumericUpDown.WinUI.BorderBrushBottom}" />
        <Setter Property="mah:ControlsHelper.CornerRadius" Value="{DynamicResource MahApps.CornerRadius.WinUI.Control}" />
        <Setter Property="mah:ControlsHelper.DisabledBorderBrush" Value="{DynamicResource MahApps.Brushes.NumericUpDown.WinUI.BorderBrushDisabled}" />
        <Setter Property="mah:ControlsHelper.FocusBorderBrush" Value="{DynamicResource MahApps.Brushes.NumericUpDown.WinUI.BorderBrushBottomFocused}" />
        <!--  the frame of a WinUI box says nothing about the pointer, which the fill behind it does instead  -->
        <Setter Property="mah:ControlsHelper.MouseOverBorderBrush" Value="{DynamicResource MahApps.Brushes.NumericUpDown.WinUI.BorderBrush}" />
        <Setter Property="mah:TextBoxHelper.ButtonTemplate" Value="{DynamicResource MahApps.Templates.Button.TextControl.WinUI}" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.NumericUpDown.WinUI.BackgroundPointerOver}" />
            </Trigger>
            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.NumericUpDown.WinUI.BackgroundFocused}" />
                <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.NumericUpDown.WinUI.ForegroundFocused}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.NumericUpDown.WinUI.BackgroundDisabled}" />
                <Setter Property="BorderBrush" Value="{DynamicResource MahApps.Brushes.NumericUpDown.WinUI.BorderBrushDisabled}" />
                <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.NumericUpDown.WinUI.ForegroundDisabled}" />
                <Setter Property="mah:ControlsHelper.BottomBorderBrush" Value="{DynamicResource MahApps.Brushes.NumericUpDown.WinUI.BorderBrushDisabled}" />
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>