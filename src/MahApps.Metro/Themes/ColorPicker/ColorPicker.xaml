<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controlzex="urn:controlzex"
                    xmlns:lang="clr-namespace:MahApps.Metro.Lang"
                    xmlns:mah="clr-namespace:MahApps.Metro.Controls"
                    xmlns:mahConverters="clr-namespace:MahApps.Metro.Converters">

    <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

    <DataTemplate x:Key="MahApps.Templates.ColorPickerContent.ColorAndName">
        <Grid x:Name="RootGrid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Width="{Binding RelativeSource={RelativeSource Mode=Self}, Path=ActualHeight, Mode=OneWay}"
                    Background="{DynamicResource MahApps.Brushes.Tile.Small}"
                    BorderBrush="{DynamicResource MahApps.Brushes.Control.Border}"
                    BorderThickness="1"
                    CornerRadius="{Binding Path=(mah:ControlsHelper.CornerRadius), RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=mah:ColorPicker}}">
                <Border Background="{Binding Converter={x:Static mahConverters:ColorToSolidColorBrushConverter.DefaultInstance}}" CornerRadius="{Binding Path=(mah:ControlsHelper.CornerRadius), RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=mah:ColorPicker}}" />
            </Border>

            <TextBlock Grid.Column="1" Margin="2 0 0 0">
                <TextBlock.Text>
                    <MultiBinding Converter="{mahConverters:ColorToNameConverter}">
                        <Binding />
                        <Binding Path="ColorNamesDictionary" RelativeSource="{RelativeSource Mode=FindAncestor, AncestorType=mah:ColorPicker}" />
                        <Binding Path="ColorHelper" RelativeSource="{RelativeSource Mode=FindAncestor, AncestorType=mah:ColorPicker}" />
                        <Binding Path="IsAlphaChannelVisible" RelativeSource="{RelativeSource Mode=FindAncestor, AncestorType=mah:ColorPicker}" />
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>

        </Grid>
        <DataTemplate.Triggers>
            <DataTrigger Binding="{Binding}" Value="{x:Null}">
                <Setter TargetName="RootGrid" Property="Visibility" Value="Collapsed" />
            </DataTrigger>
        </DataTemplate.Triggers>
    </DataTemplate>

    <DataTemplate x:Key="MahApps.Templates.ColorPickerContent.ColorOnly">
        <Border x:Name="Border"
                Width="{DynamicResource MahApps.Sizes.ColorListBox.ItemWidth}"
                Height="{DynamicResource MahApps.Sizes.ColorListBox.ItemHeight}"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                Background="{DynamicResource MahApps.Brushes.Tile.Small}"
                BorderBrush="{DynamicResource MahApps.Brushes.Control.Border}"
                BorderThickness="1"
                CornerRadius="{Binding Path=(mah:ControlsHelper.CornerRadius), RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=mah:ColorPicker}}">
            <Border Background="{Binding Converter={x:Static mahConverters:ColorToSolidColorBrushConverter.DefaultInstance}}" CornerRadius="{Binding Path=(mah:ControlsHelper.CornerRadius), RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=mah:ColorPicker}}" />
        </Border>
        <DataTemplate.Triggers>
            <DataTrigger Binding="{Binding}" Value="{x:Null}">
                <Setter TargetName="Border" Property="Visibility" Value="Collapsed" />
            </DataTrigger>
        </DataTemplate.Triggers>
    </DataTemplate>

    <!--
        The button the palettes come down behind. It is the one the combo box has, because a box a
        colour is picked in is that box with a swatch where the one pick would stand, so a set that
        draws a chevron of its own draws it here too.
    -->
    <Style x:Key="MahApps.Styles.ToggleButton.ColorPickerDropDown"
           BasedOn="{StaticResource MahApps.Styles.ToggleButton.ComboBoxDropDown}"
           TargetType="{x:Type ToggleButton}" />

    <Style x:Key="MahApps.Styles.ColorPicker" TargetType="{x:Type mah:ColorPicker}">
        <Setter Property="AddToRecentColorsTrigger" Value="ColorPickerClosed" />
        <Setter Property="AvailableColorPaletteHeader" Value="Available Colors" />
        <Setter Property="AvailableColorPaletteItemsSource" Value="{x:Static mah:BuildInColorPalettes.WpfColorsPalette}" />
        <Setter Property="AvailableColorPaletteStyle" Value="{DynamicResource MahApps.Styles.ColorPalette.ColorPickerDropDown}" />
        <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Control.Background}" />
        <Setter Property="BorderBrush" Value="{DynamicResource MahApps.Brushes.TextBox.Border}" />
        <Setter Property="BorderThickness" Value="{DynamicResource ComboBoxBorderThemeThickness}" />
        <Setter Property="CustomColorPalette01Header" Value="Custom Colors" />
        <Setter Property="CustomColorPalette01Style" Value="{DynamicResource MahApps.Styles.ColorPalette.ColorPickerDropDown}" />
        <Setter Property="CustomColorPalette02Header" Value="Custom Colors 2" />
        <Setter Property="CustomColorPalette02Style" Value="{DynamicResource MahApps.Styles.ColorPalette.ColorPickerDropDown}" />
        <Setter Property="DropDownHeight" Value="360" />
        <Setter Property="DropDownWidth" Value="500" />
        <Setter Property="FontFamily" Value="{DynamicResource MahApps.Fonts.Family.Control}" />
        <Setter Property="FontSize" Value="{DynamicResource MahApps.Font.Size.Content}" />
        <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.Text}" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="IsAdvancedTabVisible" Value="True" />
        <Setter Property="IsAvailableColorPaletteVisible" Value="True" />
        <Setter Property="IsColorPalettesTabVisible" Value="True" />
        <Setter Property="IsCustomColorPalette01Visible" Value="False" />
        <Setter Property="IsCustomColorPalette02Visible" Value="False" />
        <Setter Property="IsRecentColorPaletteVisible" Value="True" />
        <Setter Property="IsStandardColorPaletteVisible" Value="True" />
        <Setter Property="MinHeight" Value="26" />
        <Setter Property="Padding" Value="4" />
        <Setter Property="RecentColorPaletteHeader" Value="Recent Colors" />
        <Setter Property="RecentColorPaletteItemsSource" Value="{x:Static mah:BuildInColorPalettes.RecentColors}" />
        <Setter Property="RecentColorPaletteStyle" Value="{DynamicResource MahApps.Styles.ColorPalette.ColorPickerDropDown}" />
        <Setter Property="RenderOptions.ClearTypeHint" Value="Enabled" />
        <Setter Property="ScrollViewer.CanContentScroll" Value="True" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="SelectedColorTemplate" Value="{DynamicResource MahApps.Templates.ColorPickerContent.ColorAndName}" />
        <!--  change SnapsToDevicePixels to true to view a better border and validation error  -->
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="StandardColorPaletteHeader" Value="Standard Colors" />
        <Setter Property="StandardColorPaletteItemsSource" Value="{x:Static mah:BuildInColorPalettes.StandardColorsPalette}" />
        <Setter Property="StandardColorPaletteStyle" Value="{DynamicResource MahApps.Styles.ColorPalette.ColorPickerDropDown}" />
        <Setter Property="TabControlStyle" Value="{DynamicResource MahApps.Styles.TabControl.ColorPicker}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type mah:ColorPicker}">
                    <!--  whole device pixels for everything inside the frame, the way Controls.TextBox.xaml does it  -->
                    <Grid UseLayoutRounding="True">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{TemplateBinding mah:ControlsHelper.CornerRadius}"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                            <Grid x:Name="PART_InnerGrid">
                                <Grid.Clip>
                                    <!--  a clip lives in the coordinates of the element it sits on, so it takes the size of this grid and the corners the border leaves over  -->
                                    <MultiBinding Converter="{x:Static mahConverters:ClipGeometryConverter.Instance}">
                                        <Binding ElementName="PART_InnerGrid" Path="ActualWidth" />
                                        <Binding ElementName="PART_InnerGrid" Path="ActualHeight" />
                                        <Binding ElementName="Border" Path="CornerRadius" />
                                        <Binding ElementName="Border" Path="BorderThickness" />
                                        <Binding ElementName="Border" Path="Padding" />
                                    </MultiBinding>
                                </Grid.Clip>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition x:Name="TextColumn" Width="*" />
                                    <ColumnDefinition x:Name="ButtonColumn" Width="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(mah:TextBoxHelper.ButtonWidth), Mode=OneWay}" />
                                    <ColumnDefinition x:Name="ToggleButtonColumn" Width="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(mah:TextBoxHelper.ButtonWidth), Mode=OneWay}" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition x:Name="ButtonRow" Height="*" />
                                </Grid.RowDefinitions>

                                <ToggleButton x:Name="PART_DropDownToggle"
                                              Grid.RowSpan="2"
                                              Grid.ColumnSpan="3"
                                              Margin="0"
                                              VerticalAlignment="Stretch"
                                              mah:ControlsHelper.CornerRadius="{TemplateBinding mah:ControlsHelper.CornerRadius}"
                                              mah:TextBoxHelper.ButtonCommand="{TemplateBinding mah:TextBoxHelper.ButtonCommand}"
                                              mah:TextBoxHelper.ButtonCommandParameter="{TemplateBinding mah:TextBoxHelper.ButtonCommandParameter}"
                                              mah:TextBoxHelper.ButtonCommandTarget="{TemplateBinding mah:TextBoxHelper.ButtonCommandTarget}"
                                              mah:TextBoxHelper.ButtonContent="{TemplateBinding mah:TextBoxHelper.ButtonContent}"
                                              mah:TextBoxHelper.ButtonContentTemplate="{TemplateBinding mah:TextBoxHelper.ButtonContentTemplate}"
                                              mah:TextBoxHelper.ButtonFontFamily="{TemplateBinding mah:TextBoxHelper.ButtonFontFamily}"
                                              mah:TextBoxHelper.ButtonFontSize="{TemplateBinding mah:TextBoxHelper.ButtonFontSize}"
                                              mah:TextBoxHelper.ButtonTemplate="{TemplateBinding mah:TextBoxHelper.ButtonTemplate}"
                                              mah:TextBoxHelper.ButtonWidth="{TemplateBinding mah:TextBoxHelper.ButtonWidth}"
                                              mah:TextBoxHelper.ClearTextButton="{TemplateBinding mah:TextBoxHelper.ClearTextButton}"
                                              Background="Transparent"
                                              BorderThickness="0"
                                              Foreground="{TemplateBinding Foreground}"
                                              IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                              KeyboardNavigation.IsTabStop="False"
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                              Style="{DynamicResource MahApps.Styles.ToggleButton.ColorPickerDropDown}" />

                                <Grid x:Name="ContentSite"
                                      Grid.Row="1"
                                      Grid.Column="0"
                                      Margin="1 0">
                                    <ContentPresenter Margin="{TemplateBinding Padding}"
                                                      HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                      Content="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=SelectedColor, ValidatesOnExceptions=True}"
                                                      ContentTemplate="{TemplateBinding SelectedColorTemplate}"
                                                      IsHitTestVisible="False"
                                                      SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                </Grid>

                                <TextBlock x:Name="PART_Message"
                                           Grid.Row="1"
                                           Grid.Column="0"
                                           Margin="4 0"
                                           Padding="{TemplateBinding Padding}"
                                           HorizontalAlignment="Stretch"
                                           VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                           Foreground="{TemplateBinding Foreground}"
                                           Style="{DynamicResource MahApps.Styles.TextBlock.Watermark}"
                                           Text="{TemplateBinding mah:TextBoxHelper.Watermark}"
                                           TextAlignment="{TemplateBinding mah:TextBoxHelper.WatermarkAlignment}"
                                           TextTrimming="{TemplateBinding mah:TextBoxHelper.WatermarkTrimming}"
                                           Visibility="Collapsed" />

                                <ContentControl x:Name="PART_FloatingMessageContainer"
                                                Grid.Row="0"
                                                Grid.Column="0"
                                                Margin="4 0"
                                                Style="{DynamicResource MahApps.Styles.ContentControl.FloatingMessageContainer}">
                                    <ContentControl.Height>
                                        <MultiBinding Converter="{mahConverters:MathMultiplyConverter}" FallbackValue="0">
                                            <Binding ElementName="PART_FloatingMessage"
                                                     Mode="OneWay"
                                                     Path="ActualHeight" />
                                            <Binding ElementName="PART_FloatingMessageContainer"
                                                     Mode="OneWay"
                                                     Path="Opacity" />
                                        </MultiBinding>
                                    </ContentControl.Height>
                                    <TextBlock x:Name="PART_FloatingMessage"
                                               HorizontalAlignment="Stretch"
                                               VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                               Foreground="{TemplateBinding Foreground}"
                                               Style="{DynamicResource MahApps.Styles.TextBlock.AutoCollapsing}"
                                               Text="{TemplateBinding mah:TextBoxHelper.Watermark}"
                                               TextAlignment="{TemplateBinding mah:TextBoxHelper.WatermarkAlignment}"
                                               TextTrimming="{TemplateBinding mah:TextBoxHelper.WatermarkTrimming}">
                                        <TextBlock.RenderTransform>
                                            <TranslateTransform x:Name="FloatingMessageTransform">
                                                <TranslateTransform.Y>
                                                    <MultiBinding Converter="{mahConverters:MathSubtractConverter}" FallbackValue="0">
                                                        <Binding ElementName="PART_FloatingMessage"
                                                                 Mode="OneWay"
                                                                 Path="ActualHeight" />
                                                        <Binding ElementName="PART_FloatingMessageContainer"
                                                                 Mode="OneWay"
                                                                 Path="ActualHeight" />
                                                    </MultiBinding>
                                                </TranslateTransform.Y>
                                            </TranslateTransform>
                                        </TextBlock.RenderTransform>
                                    </TextBlock>
                                </ContentControl>
                            </Grid>
                        </Border>

                        <!--
                            The edge WinUI draws a little stronger, and the accent while the box has
                            the caret. It is a frame of its own carrying nothing but that edge, so
                            that it follows the corners of the one behind it and is the same two
                            units on a box of any height. A set that leaves the brush unset draws
                            nothing there.
                        -->
                        <Border x:Name="BottomEdge"
                                BorderBrush="{TemplateBinding mah:ControlsHelper.BottomBorderBrush}"
                                BorderThickness="0 0 0 2"
                                CornerRadius="{TemplateBinding mah:ControlsHelper.CornerRadius}"
                                IsHitTestVisible="False"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />

                        <Border x:Name="DisabledVisualElement"
                                Grid.ColumnSpan="3"
                                Background="{DynamicResource MahApps.Brushes.Control.Disabled}"
                                BorderBrush="{DynamicResource MahApps.Brushes.Control.Disabled}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{TemplateBinding mah:ControlsHelper.CornerRadius}"
                                IsHitTestVisible="False"
                                Opacity="0"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                Visibility="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(mah:ControlsHelper.DisabledVisualElementVisibility), Mode=OneWay}" />

                        <!--  AllowsTransparency="true" fixes the redraw problem under windows vista/7 with a selected non aero theme  -->
                        <controlzex:PopupEx x:Name="PART_Popup"
                                            AllowsTransparency="True"
                                            Focusable="False"
                                            IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                            Placement="Bottom"
                                            PlacementTarget="{Binding ElementName=PART_DropDownToggle}"
                                            PopupAnimation="{DynamicResource {x:Static SystemParameters.ComboBoxPopupAnimationKey}}"
                                            StaysOpen="False">
                            <Grid Width="{TemplateBinding DropDownWidth}"
                                  Height="{TemplateBinding DropDownHeight}"
                                  MinWidth="{TemplateBinding MinWidth}"
                                  MinHeight="{TemplateBinding MinHeight}">
                                <Border x:Name="PopupBorder"
                                        Height="Auto"
                                        HorizontalAlignment="Stretch"
                                        Background="{TemplateBinding mah:ComboBoxHelper.DropDownBackground}"
                                        BorderBrush="{TemplateBinding mah:ComboBoxHelper.DropDownBorderBrush}"
                                        BorderThickness="{DynamicResource ComboBoxPopupBorderThemeThickness}"
                                        CornerRadius="{TemplateBinding mah:ComboBoxHelper.DropDownCornerRadius}"
                                        SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                                    <Grid x:Name="ContentGrid">
                                        <Grid.Clip>
                                            <!--  a clip lives in the coordinates of the element it sits on, so it takes the size of this grid and the corners the border leaves over  -->
                                            <MultiBinding Converter="{x:Static mahConverters:ClipGeometryConverter.Instance}">
                                                <Binding ElementName="ContentGrid" Path="ActualWidth" />
                                                <Binding ElementName="ContentGrid" Path="ActualHeight" />
                                                <Binding ElementName="PopupBorder" Path="CornerRadius" />
                                                <Binding ElementName="PopupBorder" Path="BorderThickness" />
                                                <Binding ElementName="PopupBorder" Path="Padding" />
                                            </MultiBinding>
                                        </Grid.Clip>
                                        <TabControl x:Name="PART_PopupTabControl"
                                                    ItemContainerStyle="{TemplateBinding TabItemStyle}"
                                                    Style="{TemplateBinding TabControlStyle}">
                                            <TabItem x:Name="PART_ColorPalettesTab"
                                                     Header="{TemplateBinding ColorPalettesTabHeader}"
                                                     HeaderTemplate="{TemplateBinding ColorPalettesTabHeaderTemplate}"
                                                     Visibility="{TemplateBinding IsColorPalettesTabVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                <ScrollViewer CanContentScroll="False"
                                                              HorizontalScrollBarVisibility="Disabled"
                                                              VerticalScrollBarVisibility="Auto">
                                                    <VirtualizingStackPanel>
                                                        <mah:ColorPalette x:Name="PART_ColorPaletteStandard"
                                                                          ColorHelper="{TemplateBinding ColorHelper}"
                                                                          ColorNamesDictionary="{TemplateBinding ColorNamesDictionary}"
                                                                          Header="{TemplateBinding StandardColorPaletteHeader}"
                                                                          HeaderTemplate="{TemplateBinding StandardColorPaletteHeaderTemplate}"
                                                                          IsAlphaChannelVisible="{TemplateBinding IsAlphaChannelVisible}"
                                                                          ItemsSource="{TemplateBinding StandardColorPaletteItemsSource}"
                                                                          Style="{TemplateBinding StandardColorPaletteStyle}"
                                                                          Visibility="{TemplateBinding IsStandardColorPaletteVisible, Converter={StaticResource BooleanToVisibilityConverter}}" />

                                                        <mah:ColorPalette x:Name="PART_ColorPaletteAvailable"
                                                                          ColorHelper="{TemplateBinding ColorHelper}"
                                                                          ColorNamesDictionary="{TemplateBinding ColorNamesDictionary}"
                                                                          Header="{TemplateBinding AvailableColorPaletteHeader}"
                                                                          HeaderTemplate="{TemplateBinding AvailableColorPaletteHeaderTemplate}"
                                                                          IsAlphaChannelVisible="{TemplateBinding IsAlphaChannelVisible}"
                                                                          ItemsSource="{TemplateBinding AvailableColorPaletteItemsSource}"
                                                                          Style="{TemplateBinding AvailableColorPaletteStyle}"
                                                                          Visibility="{TemplateBinding IsAvailableColorPaletteVisible, Converter={StaticResource BooleanToVisibilityConverter}}" />

                                                        <mah:ColorPalette x:Name="PART_ColorPaletteCustom01"
                                                                          ColorHelper="{TemplateBinding ColorHelper}"
                                                                          ColorNamesDictionary="{TemplateBinding ColorNamesDictionary}"
                                                                          Header="{TemplateBinding CustomColorPalette01Header}"
                                                                          HeaderTemplate="{TemplateBinding CustomColorPalette01HeaderTemplate}"
                                                                          IsAlphaChannelVisible="{TemplateBinding IsAlphaChannelVisible}"
                                                                          ItemsSource="{TemplateBinding CustomColorPalette01ItemsSource}"
                                                                          Style="{TemplateBinding CustomColorPalette01Style}"
                                                                          Visibility="{TemplateBinding IsCustomColorPalette01Visible, Converter={StaticResource BooleanToVisibilityConverter}}" />

                                                        <mah:ColorPalette x:Name="PART_ColorPaletteCustom02"
                                                                          ColorHelper="{TemplateBinding ColorHelper}"
                                                                          ColorNamesDictionary="{TemplateBinding ColorNamesDictionary}"
                                                                          Header="{TemplateBinding CustomColorPalette02Header}"
                                                                          HeaderTemplate="{TemplateBinding CustomColorPalette02HeaderTemplate}"
                                                                          IsAlphaChannelVisible="{TemplateBinding IsAlphaChannelVisible}"
                                                                          ItemsSource="{TemplateBinding CustomColorPalette02ItemsSource}"
                                                                          Style="{TemplateBinding CustomColorPalette02Style}"
                                                                          Visibility="{TemplateBinding IsCustomColorPalette02Visible, Converter={StaticResource BooleanToVisibilityConverter}}" />

                                                        <mah:ColorPalette x:Name="PART_ColorPaletteRecent"
                                                                          ColorHelper="{TemplateBinding ColorHelper}"
                                                                          ColorNamesDictionary="{TemplateBinding ColorNamesDictionary}"
                                                                          Header="{TemplateBinding RecentColorPaletteHeader}"
                                                                          HeaderTemplate="{TemplateBinding RecentColorPaletteHeaderTemplate}"
                                                                          IsAlphaChannelVisible="{TemplateBinding IsAlphaChannelVisible}"
                                                                          ItemsSource="{TemplateBinding RecentColorPaletteItemsSource}"
                                                                          Style="{TemplateBinding RecentColorPaletteStyle}"
                                                                          Visibility="{TemplateBinding IsRecentColorPaletteVisible, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                                    </VirtualizingStackPanel>
                                                </ScrollViewer>

                                            </TabItem>
                                            <TabItem x:Name="PART_AdvancedTab"
                                                     Header="{TemplateBinding AdvancedTabHeader}"
                                                     HeaderTemplate="{TemplateBinding AdvancedTabHeaderTemplate}"
                                                     Visibility="{TemplateBinding IsAdvancedTabVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                <ScrollViewer>
                                                    <mah:ColorCanvas mah:TextBoxHelper.AutoWatermark="{TemplateBinding mah:TextBoxHelper.AutoWatermark}"
                                                                     mah:TextBoxHelper.ButtonCommand="{TemplateBinding mah:TextBoxHelper.ButtonCommand}"
                                                                     mah:TextBoxHelper.ButtonCommandParameter="{TemplateBinding mah:TextBoxHelper.ButtonCommandParameter}"
                                                                     mah:TextBoxHelper.ButtonCommandTarget="{TemplateBinding mah:TextBoxHelper.ButtonCommandTarget}"
                                                                     mah:TextBoxHelper.ButtonContent="{TemplateBinding mah:TextBoxHelper.ButtonContent}"
                                                                     mah:TextBoxHelper.ButtonContentTemplate="{TemplateBinding mah:TextBoxHelper.ButtonContentTemplate}"
                                                                     mah:TextBoxHelper.ButtonFontFamily="{TemplateBinding mah:TextBoxHelper.ButtonFontFamily}"
                                                                     mah:TextBoxHelper.ButtonFontSize="{TemplateBinding mah:TextBoxHelper.ButtonFontSize}"
                                                                     mah:TextBoxHelper.ButtonTemplate="{TemplateBinding mah:TextBoxHelper.ButtonTemplate}"
                                                                     mah:TextBoxHelper.ButtonWidth="{TemplateBinding mah:TextBoxHelper.ButtonWidth}"
                                                                     mah:TextBoxHelper.ButtonsAlignment="{TemplateBinding mah:TextBoxHelper.ButtonsAlignment}"
                                                                     mah:TextBoxHelper.ClearTextButton="{TemplateBinding mah:TextBoxHelper.ClearTextButton}"
                                                                     mah:TextBoxHelper.Watermark="{TemplateBinding mah:TextBoxHelper.Watermark}"
                                                                     mah:TextBoxHelper.WatermarkAlignment="{TemplateBinding mah:TextBoxHelper.WatermarkAlignment}"
                                                                     mah:TextBoxHelper.WatermarkTrimming="{TemplateBinding mah:TextBoxHelper.WatermarkTrimming}"
                                                                     mah:TextBoxHelper.WatermarkWrapping="{TemplateBinding mah:TextBoxHelper.WatermarkWrapping}"
                                                                     AreHsvChannelsVisible="{TemplateBinding AreHsvChannelsVisible}"
                                                                     AreRgbChannelsVisible="{TemplateBinding AreRgbChannelsVisible}"
                                                                     ColorHelper="{TemplateBinding ColorHelper}"
                                                                     ColorNamesDictionary="{TemplateBinding ColorNamesDictionary}"
                                                                     IsAlphaChannelVisible="{TemplateBinding IsAlphaChannelVisible}"
                                                                     IsColorNameVisible="{TemplateBinding IsColorNameVisible}"
                                                                     IsEyeDropperVisible="{TemplateBinding IsEyeDropperVisible}"
                                                                     LabelAlphaChannel="{TemplateBinding LabelAlphaChannel}"
                                                                     LabelBlueChannel="{TemplateBinding LabelBlueChannel}"
                                                                     LabelColorName="{TemplateBinding LabelColorName}"
                                                                     LabelColorPreview="{TemplateBinding LabelColorPreview}"
                                                                     LabelGreenChannel="{TemplateBinding LabelGreenChannel}"
                                                                     LabelHueChannel="{TemplateBinding LabelHueChannel}"
                                                                     LabelRedChannel="{TemplateBinding LabelRedChannel}"
                                                                     LabelSaturationChannel="{TemplateBinding LabelSaturationChannel}"
                                                                     LabelValueChannel="{TemplateBinding LabelValueChannel}"
                                                                     SelectedColor="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=SelectedColor}" />
                                                </ScrollViewer>
                                            </TabItem>
                                        </TabControl>
                                    </Grid>
                                </Border>
                            </Grid>
                        </controlzex:PopupEx>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="FocusStates">
                                <VisualState x:Name="Focused" />
                                <VisualState x:Name="Unfocused" />
                                <VisualState x:Name="FocusedDropDown">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="PopupBorder"
                                                                       Storyboard.TargetProperty="(UIElement.Visibility)"
                                                                       Duration="00:00:00">
                                            <DiscreteObjectKeyFrame KeyTime="00:00:00">
                                                <DiscreteObjectKeyFrame.Value>
                                                    <Visibility>Visible</Visibility>
                                                </DiscreteObjectKeyFrame.Value>
                                            </DiscreteObjectKeyFrame>
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=SelectedColor}" Value="{x:Null}" />
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="PART_Message" Property="Visibility" Value="Visible" />
                        </MultiDataTrigger>

                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=SelectedColor, Mode=OneWay, Converter={x:Static mahConverters:IsNullConverter.Instance}}" Value="True" />
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsKeyboardFocusWithin}" Value="True" />
                            </MultiDataTrigger.Conditions>
                            <MultiDataTrigger.EnterActions>
                                <BeginStoryboard Storyboard="{StaticResource MahApps.Storyboard.EnterGotFocus}" />
                            </MultiDataTrigger.EnterActions>
                            <MultiDataTrigger.ExitActions>
                                <BeginStoryboard Storyboard="{StaticResource MahApps.Storyboard.ExitGotFocus}" />
                            </MultiDataTrigger.ExitActions>
                        </MultiDataTrigger>

                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=SelectedColor, Mode=OneWay, Converter={x:Static mahConverters:IsNullConverter.Instance}}" Value="False" />
                            </MultiDataTrigger.Conditions>
                            <MultiDataTrigger.EnterActions>
                                <BeginStoryboard Storyboard="{StaticResource MahApps.Storyboard.EnterHasText}" />
                            </MultiDataTrigger.EnterActions>
                            <MultiDataTrigger.ExitActions>
                                <BeginStoryboard Storyboard="{StaticResource MahApps.Storyboard.ExitHasText}" />
                            </MultiDataTrigger.ExitActions>
                        </MultiDataTrigger>

                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding Path=IsVisible, RelativeSource={RelativeSource Self}}" Value="True" />
                                <Condition Binding="{Binding Path=(mah:TextBoxHelper.UseFloatingWatermark), RelativeSource={RelativeSource Self}}" Value="True" />
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=SelectedColor, Mode=OneWay, Converter={x:Static mahConverters:IsNullConverter.Instance}}" Value="False" />
                                <Condition Binding="{Binding Path=(mah:TextBoxHelper.Watermark), RelativeSource={RelativeSource Self}, Converter={x:Static mahConverters:StringIsNullOrEmptyConverter.Default}}" Value="False" />
                            </MultiDataTrigger.Conditions>
                            <MultiDataTrigger.EnterActions>
                                <BeginStoryboard Storyboard="{StaticResource MahApps.Storyboard.ShowFloatingMessage}" />
                            </MultiDataTrigger.EnterActions>
                            <MultiDataTrigger.ExitActions>
                                <BeginStoryboard Storyboard="{StaticResource MahApps.Storyboard.HideFloatingMessage}" />
                            </MultiDataTrigger.ExitActions>
                        </MultiDataTrigger>

                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="mah:TextBoxHelper.ClearTextButton" Value="False" />
                                <Condition Property="mah:TextBoxHelper.ButtonCommand" Value="{x:Null}" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="ContentSite" Property="Grid.ColumnSpan" Value="2" />
                            <Setter TargetName="PART_Message" Property="Grid.ColumnSpan" Value="2" />
                        </MultiTrigger>

                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Gray9}" />
                            <Setter TargetName="Border" Property="BorderBrush" Value="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(mah:ControlsHelper.MouseOverBorderBrush)}" />
                        </Trigger>

                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="PART_FloatingMessage" Property="Foreground" Value="{DynamicResource MahApps.Brushes.Accent}" />
                            <Setter TargetName="PART_FloatingMessage" Property="Opacity" Value="1" />
                        </Trigger>
                        <!--
                            A set that draws an edge of its own says the focus with that edge, the way
                            WinUI does it, and the frame behind it stays the colour it was. One that
                            leaves the edge unset says it with the frame, which is what the Metro and
                            the Windows 10 box have always done.
                        -->
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter TargetName="BottomEdge" Property="BorderBrush" Value="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(mah:ControlsHelper.FocusBorderBrush)}" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsKeyboardFocusWithin" Value="True" />
                                <Condition Property="mah:ControlsHelper.BottomBorderBrush" Value="{x:Null}" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Border" Property="BorderBrush" Value="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(mah:ControlsHelper.FocusBorderBrush)}" />
                            <Setter TargetName="BottomEdge" Property="BorderBrush" Value="{x:Null}" />
                        </MultiTrigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="DisabledVisualElement" Property="Opacity" Value="0.6" />
                        </Trigger>
                        <!--
                            A set that draws no veil over a box that is switched off says it with the
                            colours instead, the way the text box of that set does: the frame and the
                            edge take the disabled stroke, and the style turns the fill and the text
                            down with them.
                        -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="False" />
                                <Condition Property="mah:ControlsHelper.DisabledVisualElementVisibility" Value="Collapsed" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Border" Property="BorderBrush" Value="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(mah:ControlsHelper.DisabledBorderBrush)}" />
                            <Setter TargetName="BottomEdge" Property="BorderBrush" Value="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(mah:ControlsHelper.DisabledBorderBrush)}" />
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Validation.ErrorTemplate" Value="{DynamicResource MahApps.Templates.ValidationError}" />
        <Setter Property="VerticalContentAlignment" Value="Center" />

        <Setter Property="mah:BuildInColorPalettes.MaximumRecentColorsCount" Value="18" />

        <Setter Property="mah:ComboBoxHelper.DropDownBackground" Value="{DynamicResource MahApps.Brushes.ComboBox.PopupBackground}" />
        <Setter Property="mah:ComboBoxHelper.DropDownBorderBrush" Value="{DynamicResource MahApps.Brushes.ComboBox.PopupBorder}" />
        <Setter Property="mah:ComboBoxHelper.DropDownCornerRadius" Value="{DynamicResource ComboBoxPopupBorderThemeCornerRadius}" />

        <Setter Property="mah:ControlsHelper.DisabledBorderBrush" Value="{DynamicResource MahApps.Brushes.Control.Disabled}" />
        <Setter Property="mah:ControlsHelper.FocusBorderBrush" Value="{DynamicResource MahApps.Brushes.ComboBox.Border.Focus}" />
        <Setter Property="mah:ControlsHelper.MouseOverBorderBrush" Value="{DynamicResource MahApps.Brushes.ComboBox.Border.MouseOver}" />

        <Setter Property="mah:TextBoxHelper.ButtonCommandParameter" Value="{Binding RelativeSource={RelativeSource Self}, Mode=OneWay}" />
        <Setter Property="mah:TextBoxHelper.ButtonCommandTarget" Value="{Binding RelativeSource={RelativeSource Self}, Mode=OneWay}" />
        <Setter Property="mah:TextBoxHelper.ButtonFontSize" Value="{DynamicResource MahApps.Font.Size.Button.ClearText}" />
        <Setter Property="mah:TextBoxHelper.ButtonTemplate" Value="{DynamicResource MahApps.Templates.Button.Chromeless}" />
        <Setter Property="mah:TextBoxHelper.ButtonWidth" Value="22" />
    </Style>

    <!--
        The Windows 10 picker. A box a colour is picked in is the combo box of that set with a swatch
        and a name where the one pick would stand, so it wears that box: the same fill, the same two
        units of frame, the same padding, the same chevron. What comes down behind it is the flyout of
        that set with the palettes and the canvas of it inside, and both of those sit in
        Style.Resources rather than in the application, so a picker wears the set on its own.
        Switched off it says so with its colours rather than with a veil over it, the same way.
    -->
    <Style x:Key="MahApps.Styles.ColorPicker.Win10"
           BasedOn="{StaticResource MahApps.Styles.ColorPicker}"
           TargetType="{x:Type mah:ColorPicker}">
        <Style.Resources>
            <Style x:Key="MahApps.Styles.ToggleButton.ColorPickerDropDown"
                   BasedOn="{StaticResource MahApps.Styles.ToggleButton.ComboBoxDropDown.Win10}"
                   TargetType="{x:Type ToggleButton}" />
            <Style BasedOn="{StaticResource MahApps.Styles.ColorCanvas.Win10}" TargetType="{x:Type mah:ColorCanvas}" />
        </Style.Resources>
        <Setter Property="AvailableColorPaletteStyle" Value="{StaticResource MahApps.Styles.ColorPalette.ColorPickerDropDown.Win10}" />
        <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.ComboBox.Win10.Background}" />
        <Setter Property="BorderBrush" Value="{DynamicResource MahApps.Brushes.ComboBox.Win10.BorderBrush}" />
        <Setter Property="BorderThickness" Value="2" />
        <Setter Property="CustomColorPalette01Style" Value="{StaticResource MahApps.Styles.ColorPalette.ColorPickerDropDown.Win10}" />
        <Setter Property="CustomColorPalette02Style" Value="{StaticResource MahApps.Styles.ColorPalette.ColorPickerDropDown.Win10}" />
        <!--  the rows in there are a set taller, so what holds them is taller by about that much  -->
        <Setter Property="DropDownHeight" Value="400" />
        <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.ComboBox.Win10.Foreground}" />
        <Setter Property="MinHeight" Value="32" />
        <Setter Property="Padding" Value="10 6 6 5" />
        <Setter Property="RecentColorPaletteStyle" Value="{StaticResource MahApps.Styles.ColorPalette.ColorPickerDropDown.Win10}" />
        <Setter Property="StandardColorPaletteStyle" Value="{StaticResource MahApps.Styles.ColorPalette.ColorPickerDropDown.Win10}" />
        <Setter Property="mah:ComboBoxHelper.DropDownBackground" Value="{DynamicResource MahApps.Brushes.ComboBox.Win10.DropDownBackground}" />
        <Setter Property="mah:ComboBoxHelper.DropDownBorderBrush" Value="{DynamicResource MahApps.Brushes.ComboBox.Win10.DropDownBorderBrush}" />
        <Setter Property="mah:ControlsHelper.DisabledBorderBrush" Value="{DynamicResource MahApps.Brushes.ComboBox.Win10.BorderBrushDisabled}" />
        <!--  no veil over a picker that is off: this set says it with the colours, the way its text box does  -->
        <Setter Property="mah:ControlsHelper.DisabledVisualElementVisibility" Value="Collapsed" />
        <Setter Property="mah:ControlsHelper.FocusBorderBrush" Value="{DynamicResource MahApps.Brushes.ComboBox.Win10.BorderBrushFocused}" />
        <Setter Property="mah:ControlsHelper.MouseOverBorderBrush" Value="{DynamicResource MahApps.Brushes.ComboBox.Win10.BorderBrushPointerOver}" />
        <Setter Property="mah:TextBoxHelper.ButtonContent" Value="&#xE10A;" />
        <Setter Property="mah:TextBoxHelper.ButtonContentTemplate">
            <Setter.Value>
                <DataTemplate>
                    <mah:FontIcon FontFamily="{DynamicResource MahApps.Fonts.Family.SymbolTheme}"
                                  FontSize="12"
                                  Glyph="{Binding}"
                                  UseLayoutRounding="False" />
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="mah:TextBoxHelper.ButtonTemplate" Value="{DynamicResource MahApps.Templates.Button.TextControl.Win10}" />
        <Setter Property="mah:TextBoxHelper.ButtonWidth" Value="30" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.ComboBox.Win10.BackgroundPointerOver}" />
            </Trigger>
            <!--  while the palettes are down the box stays in the colour it takes under the finger  -->
            <Trigger Property="IsDropDownOpen" Value="True">
                <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.ComboBox.Win10.BackgroundPressed}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.ComboBox.Win10.BackgroundDisabled}" />
                <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.ComboBox.Win10.ForegroundDisabled}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!--
        WinUI on top of it, the way its combo box has it: the line along the bottom edge is the thick
        one and turns into the accent while the box has the caret, and the corners are rounded. What
        comes down is rounded a little more, which is what a flyout does there, and the swatches in it
        are rounded with the boxes beside them.
    -->
    <Style x:Key="MahApps.Styles.ColorPicker.WinUI"
           BasedOn="{StaticResource MahApps.Styles.ColorPicker.Win10}"
           TargetType="{x:Type mah:ColorPicker}">
        <Style.Resources>
            <Style x:Key="MahApps.Styles.ToggleButton.ColorPickerDropDown"
                   BasedOn="{StaticResource MahApps.Styles.ToggleButton.ComboBoxDropDown.Win10}"
                   TargetType="{x:Type ToggleButton}" />
            <Style BasedOn="{StaticResource MahApps.Styles.ColorCanvas.WinUI}" TargetType="{x:Type mah:ColorCanvas}" />
        </Style.Resources>
        <Setter Property="AvailableColorPaletteStyle" Value="{StaticResource MahApps.Styles.ColorPalette.ColorPickerDropDown.WinUI}" />
        <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.ComboBox.WinUI.Background}" />
        <Setter Property="BorderBrush" Value="{DynamicResource MahApps.Brushes.ComboBox.WinUI.BorderBrush}" />
        <Setter Property="BorderThickness" Value="1 1 1 2" />
        <Setter Property="CustomColorPalette01Style" Value="{StaticResource MahApps.Styles.ColorPalette.ColorPickerDropDown.WinUI}" />
        <Setter Property="CustomColorPalette02Style" Value="{StaticResource MahApps.Styles.ColorPalette.ColorPickerDropDown.WinUI}" />
        <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.ComboBox.WinUI.Foreground}" />
        <Setter Property="RecentColorPaletteStyle" Value="{StaticResource MahApps.Styles.ColorPalette.ColorPickerDropDown.WinUI}" />
        <Setter Property="StandardColorPaletteStyle" Value="{StaticResource MahApps.Styles.ColorPalette.ColorPickerDropDown.WinUI}" />
        <Setter Property="mah:ComboBoxHelper.DropDownBackground" Value="{DynamicResource MahApps.Brushes.ComboBox.WinUI.DropDownBackground}" />
        <Setter Property="mah:ComboBoxHelper.DropDownBorderBrush" Value="{DynamicResource MahApps.Brushes.ComboBox.WinUI.DropDownBorderBrush}" />
        <Setter Property="mah:ComboBoxHelper.DropDownCornerRadius" Value="{DynamicResource MahApps.CornerRadius.WinUI.Overlay}" />
        <Setter Property="mah:ControlsHelper.BottomBorderBrush" Value="{DynamicResource MahApps.Brushes.ComboBox.WinUI.BorderBrushBottom}" />
        <Setter Property="mah:ControlsHelper.CornerRadius" Value="{DynamicResource MahApps.CornerRadius.WinUI.Control}" />
        <Setter Property="mah:ControlsHelper.DisabledBorderBrush" Value="{DynamicResource MahApps.Brushes.ComboBox.WinUI.BorderBrushDisabled}" />
        <Setter Property="mah:ControlsHelper.FocusBorderBrush" Value="{DynamicResource MahApps.Brushes.ComboBox.WinUI.BorderBrushBottomFocused}" />
        <!--  the frame of a WinUI box says nothing about the pointer, which the fill behind it does instead  -->
        <Setter Property="mah:ControlsHelper.MouseOverBorderBrush" Value="{DynamicResource MahApps.Brushes.ComboBox.WinUI.BorderBrush}" />
        <Setter Property="mah:TextBoxHelper.ButtonTemplate" Value="{DynamicResource MahApps.Templates.Button.TextControl.WinUI}" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.ComboBox.WinUI.BackgroundPointerOver}" />
            </Trigger>
            <Trigger Property="IsDropDownOpen" Value="True">
                <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.ComboBox.WinUI.BackgroundPointerOver}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.ComboBox.WinUI.BackgroundDisabled}" />
                <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.ComboBox.WinUI.ForegroundDisabled}" />
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>