<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:mah="clr-namespace:MahApps.Metro.Controls"
                    xmlns:mahConverters="clr-namespace:MahApps.Metro.Converters"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    xmlns:po="http://schemas.microsoft.com/winfx/2006/xaml/presentation/options"
                    mc:Ignorable="po">

    <Cursor x:Key="MahApps.Cursors.EyeDropper">pack://application:,,,/MahApps.Metro;component/Assets/Eyedropper.cur</Cursor>

    <Style x:Key="MahApps.Styles.ColorEyeDropper"
           BasedOn="{StaticResource {x:Type Button}}"
           TargetType="{x:Type mah:ColorEyeDropper}">
        <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Gray10}" />
        <Setter Property="BorderBrush" Value="{DynamicResource MahApps.Brushes.Button.Border}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="EyeDropperCursor" Value="{StaticResource MahApps.Cursors.EyeDropper}" />
        <Setter Property="FontFamily" Value="{DynamicResource MahApps.Fonts.Family.Button}" />
        <Setter Property="FontSize" Value="{DynamicResource MahApps.Font.Size.Button}" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.ThemeForeground}" />
        <!--  the pipette stands in the middle of the button however tall the row it is stretched into  -->
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="MinHeight" Value="25" />
        <Setter Property="Padding" Value="5 6" />
        <Setter Property="PreviewContentTemplate">
            <Setter.Value>
                <DataTemplate DataType="{x:Type mah:ColorEyePreviewData}">
                    <Grid MinWidth="110">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0"
                                Grid.ColumnSpan="2"
                                HorizontalAlignment="Center"
                                BorderBrush="{DynamicResource MahApps.Brushes.Gray}"
                                BorderThickness="1">
                            <Grid>
                                <Image x:Name="PART_PreviewImage"
                                       Width="50"
                                       Height="50"
                                       RenderOptions.BitmapScalingMode="NearestNeighbor"
                                       Source="{Binding PreviewImage}" />

                                <Path Stroke="{Binding PreviewBrush, Converter={x:Static mahConverters:BackgroundToForegroundConverter.Instance}}" StrokeThickness="1">
                                    <Path.Data>
                                        <PathGeometry Figures=" m 0 25 20 0  m 5 5 0 20  m 5 -25 20 0  m -25 -25 0 20 m -5 0 H20 H30 V30 H20 z" po:Freeze="true" />
                                    </Path.Data>
                                </Path>

                            </Grid>
                        </Border>

                        <Ellipse x:Name="PART_EllipsePreviewBrush"
                                 Grid.Row="1"
                                 Grid.Column="0"
                                 Width="{DynamicResource MahApps.Font.Size.Content}"
                                 Height="{DynamicResource MahApps.Font.Size.Content}"
                                 Margin="5"
                                 Fill="{Binding PreviewBrush}"
                                 Stroke="{DynamicResource MahApps.Brushes.Gray}"
                                 StrokeThickness="1" />

                        <TextBlock Grid.Row="1"
                                   Grid.Column="1"
                                   Margin="5"
                                   Text="{Binding PreviewBrush}" />
                    </Grid>
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ButtonBase}">
                    <Grid>
                        <Border x:Name="Border"
                                Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{TemplateBinding mah:ControlsHelper.CornerRadius}"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                            <Grid x:Name="ContentGrid">
                                <Grid.Clip>
                                    <!--  a clip lives in the coordinates of the element it sits on, so it takes the size of this grid and the corners the border leaves over  -->
                                    <MultiBinding Converter="{x:Static mahConverters:ClipGeometryConverter.Instance}">
                                        <Binding ElementName="ContentGrid" Path="ActualWidth" />
                                        <Binding ElementName="ContentGrid" Path="ActualHeight" />
                                        <Binding ElementName="Border" Path="CornerRadius" />
                                        <Binding ElementName="Border" Path="BorderThickness" />
                                        <Binding ElementName="Border" Path="Padding" />
                                    </MultiBinding>
                                </Grid.Clip>
                                <mah:ContentControlEx x:Name="PART_ContentPresenter"
                                                      HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                      VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                                      Content="{TemplateBinding Content}"
                                                      ContentCharacterCasing="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(mah:ControlsHelper.ContentCharacterCasing)}"
                                                      ContentStringFormat="{TemplateBinding ContentStringFormat}"
                                                      ContentTemplate="{TemplateBinding ContentTemplate}"
                                                      ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
                                                      RecognizesAccessKey="{TemplateBinding mah:ControlsHelper.RecognizesAccessKey}"
                                                      SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                            </Grid>
                        </Border>
                        <Border x:Name="DisabledVisualElement"
                                Background="{DynamicResource MahApps.Brushes.Control.Disabled}"
                                CornerRadius="{TemplateBinding mah:ControlsHelper.CornerRadius}"
                                IsHitTestVisible="False"
                                Opacity="0"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                Visibility="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(mah:ControlsHelper.DisabledVisualElementVisibility), Mode=OneWay}" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <!--
                            What the frame is filled with under the pointer and under the finger is the
                            style's to say rather than the template's, so that a set can say something
                            else. A trigger in here would beat one in a style standing on this template.
                        -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="BorderBrush" Value="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(mah:ControlsHelper.MouseOverBorderBrush), Mode=OneWay}" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter TargetName="Border" Property="BorderBrush" Value="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(mah:ControlsHelper.FocusBorderBrush), Mode=OneWay}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="DisabledVisualElement" Property="Opacity" Value="0.7" />
                            <Setter TargetName="PART_ContentPresenter" Property="Opacity" Value="0.4" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="mah:ControlsHelper.ContentCharacterCasing" Value="{DynamicResource MahApps.CharacterCasing.ColorEyeDropper}" />
        <Setter Property="mah:ControlsHelper.CornerRadius" Value="3" />
        <Setter Property="mah:ControlsHelper.FocusBorderBrush" Value="{DynamicResource MahApps.Brushes.Button.Border.Focus}" />
        <Setter Property="mah:ControlsHelper.MouseOverBorderBrush" Value="{DynamicResource MahApps.Brushes.Button.Border.MouseOver}" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Gray8}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Gray7}" />
            </Trigger>
            <Trigger Property="Content" Value="{x:Null}">
                <Setter Property="ContentTemplate">
                    <Setter.Value>
                        <DataTemplate>
                            <Viewbox Width="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Button}, Path=FontSize}"
                                     Height="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Button}, Path=FontSize}"
                                     Stretch="Uniform">
                                <Canvas Width="24" Height="24">
                                    <Path Data="M19.35,11.72L17.22,13.85L15.81,12.43L8.1,20.14L3.5,22L2,20.5L3.86,15.9L11.57,8.19L10.15,6.78L12.28,4.65L19.35,11.72M16.76,3C17.93,1.83 19.83,1.83 21,3C22.17,4.17 22.17,6.07 21,7.24L19.08,9.16L14.84,4.92L16.76,3M5.56,17.03L4.5,19.5L6.97,18.44L14.4,11L13,9.6L5.56,17.03Z" Fill="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Button}, Path=Foreground}" />
                                </Canvas>
                            </Viewbox>
                        </DataTemplate>
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="MahApps.Styles.ColorEyeDropper.ColorCanvas"
           BasedOn="{StaticResource MahApps.Styles.ColorEyeDropper}"
           TargetType="{x:Type mah:ColorEyeDropper}">
        <Setter Property="mah:ControlsHelper.CornerRadius" Value="0" />
    </Style>

    <!--
        The dropper of the two Windows sets, which is the button of the set: its fill, its two units
        of frame, its padding and its colours under the pointer and under the finger. Switched off it
        says so with those colours rather than with a veil over it, the way that button does. The
        padding and the thickness are the numbers MahApps.Styles.Button.Win10 keeps in its own
        resources, which are its to scope rather than the dictionary's to share.
    -->
    <Style x:Key="MahApps.Styles.ColorEyeDropper.Win10"
           BasedOn="{StaticResource MahApps.Styles.ColorEyeDropper}"
           TargetType="{x:Type mah:ColorEyeDropper}">
        <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Button.Background}" />
        <Setter Property="BorderBrush" Value="{DynamicResource MahApps.Brushes.Button.BorderBrush}" />
        <Setter Property="BorderThickness" Value="2" />
        <Setter Property="FontFamily" Value="{DynamicResource MahApps.Fonts.Family.ContentControlTheme}" />
        <Setter Property="FontSize" Value="{DynamicResource MahApps.Font.Size.ControlContentTheme}" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.Button.Foreground}" />
        <!--
            The eight either side are what that button leaves; the four above and below are not,
            because what stands in here is a pipette rather than a line of text and an icon wants the
            same air above it as under it.
        -->
        <Setter Property="Padding" Value="8 4" />
        <Setter Property="mah:ControlsHelper.CornerRadius" Value="0" />
        <!--  no veil over a dropper that is off: this set says it with the colours, the way its button does  -->
        <Setter Property="mah:ControlsHelper.DisabledVisualElementVisibility" Value="Collapsed" />
        <Setter Property="mah:ControlsHelper.MouseOverBorderBrush" Value="{DynamicResource MahApps.Brushes.Button.BorderBrushPointerOver}" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Button.BackgroundPointerOver}" />
                <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.Button.ForegroundPointerOver}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Button.BackgroundPressed}" />
                <Setter Property="BorderBrush" Value="{DynamicResource MahApps.Brushes.Button.BorderBrushPressed}" />
                <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.Button.ForegroundPressed}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Button.BackgroundDisabled}" />
                <Setter Property="BorderBrush" Value="{DynamicResource MahApps.Brushes.Button.BorderBrushDisabled}" />
                <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.Button.ForegroundDisabled}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!--
        The WinUI set has no button of its own yet and hands the Windows 10 one down, so the dropper
        of that set is this one with the corners the set rounds by: it stands against the box the
        colour is named in, and a square button beside a rounded box reads as a mistake.
    -->
    <Style x:Key="MahApps.Styles.ColorEyeDropper.WinUI"
           BasedOn="{StaticResource MahApps.Styles.ColorEyeDropper.Win10}"
           TargetType="{x:Type mah:ColorEyeDropper}">
        <Setter Property="mah:ControlsHelper.CornerRadius" Value="{DynamicResource MahApps.CornerRadius.WinUI.Control}" />
    </Style>

    <!--
        The dropper on a canvas of either set. The Metro one takes its corners off, because it stands
        against a square box there; these two already carry the corners of the box beside them, so
        there is nothing left for the canvas to take off.
    -->
    <Style x:Key="MahApps.Styles.ColorEyeDropper.ColorCanvas.Win10"
           BasedOn="{StaticResource MahApps.Styles.ColorEyeDropper.Win10}"
           TargetType="{x:Type mah:ColorEyeDropper}" />

    <Style x:Key="MahApps.Styles.ColorEyeDropper.ColorCanvas.WinUI"
           BasedOn="{StaticResource MahApps.Styles.ColorEyeDropper.WinUI}"
           TargetType="{x:Type mah:ColorEyeDropper}" />

</ResourceDictionary>