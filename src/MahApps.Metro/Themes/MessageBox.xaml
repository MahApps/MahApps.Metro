<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:options="http://schemas.microsoft.com/winfx/2006/xaml/presentation/options"
                    xmlns:local="clr-namespace:MahApps.Metro.Controls">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.Buttons.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="MessageBoxButtonStyle" TargetType="Button" BasedOn="{StaticResource MahApps.Metro.Styles.MetroButton}">
        <Setter Property="MinWidth" Value="80"/>
        <Setter Property="MinHeight" Value="28"/>
        <Setter Property="Margin" Value="2,4,2,4"/>
        <Style.Triggers>
            <Trigger Property="IsFocused" Value="true">
                <Setter Property="BorderBrush" Value="{DynamicResource AccentColorBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>


    <Style TargetType="local:MessageBox">
        <Setter Property="MinWidth" Value="280"/>
        <Setter Property="MinHeight" Value="150"/>
        <Setter Property="MaxWidth" Value="400"/>
        <Setter Property="WindowChrome.WindowChrome">
            <Setter.Value>
                <WindowChrome GlassFrameThickness="-1" ResizeBorderThickness="2" CaptionHeight="36" CornerRadius="0"/>
            </Setter.Value>
        </Setter>
        <Setter Property="WindowStyle" Value="None"/>
        <Setter Property="ResizeMode" Value="NoResize"/>
        <Setter Property="CloseButtonStyle">
            <Setter.Value>
                <Style TargetType="Button">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="1"/>
                    <Setter Property="BorderBrush" Value="Transparent"/>
                    <Setter Property="Foreground" Value="{DynamicResource Foreground}"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            Margin="{TemplateBinding Margin}"
                                            Padding="{TemplateBinding Padding}">
                                    <ContentPresenter/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="Red"/>
                            <Setter Property="Foreground" Value="{DynamicResource AccentColorBrush}"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Setter.Value>
        </Setter>
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
        <Setter Property="OkButtonStyle" Value="{StaticResource MessageBoxButtonStyle}"/>
        <Setter Property="YesButtonStyle" Value="{StaticResource MessageBoxButtonStyle}"/>
        <Setter Property="NoButtonStyle" Value="{StaticResource MessageBoxButtonStyle}"/>
        <Setter Property="CancelButtonStyle" Value="{StaticResource MessageBoxButtonStyle}"/>
        <Setter Property="WindowTitleBrush" Value="{DynamicResource WindowTitleColorBrush}"/>
        <Setter Property="WindowContentBrush" Value="{DynamicResource ControlBackgroundBrush}"/>
        <Setter Property="MessageBoxButtonBackground" Value="{DynamicResource ControlBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource AccentBaseColorBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:MessageBox}">
                    <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="36"/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            <Grid Background="{TemplateBinding WindowTitleBrush}">
                                <TextBlock Text="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Title}" 
                                           VerticalAlignment="Top" HorizontalAlignment="Left"  Margin="8,8,0,0" FontSize="18"/>
                                <Button x:Name="PART_CloseButton"  HorizontalAlignment="Right" Width="46" Focusable="False" UseLayoutRounding="True"
                                            Style="{TemplateBinding CloseButtonStyle}" WindowChrome.IsHitTestVisibleInChrome="True">
                                    <Grid>
                                        <Line HorizontalAlignment="Center" VerticalAlignment="Center" SnapsToDevicePixels="True"
                                                  Stroke="{Binding Path=Foreground, RelativeSource={RelativeSource AncestorType={x:Type Button}}}"
                                                  X1="0"
                                                  X2="10"
                                                  Y1="0"
                                                  Y2="10" />
                                        <Line HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"
                                                  SnapsToDevicePixels="True"
                                                  Stroke="{Binding Path=Foreground, RelativeSource={RelativeSource AncestorType={x:Type Button}}}"
                                                  X1="0"
                                                  X2="10"
                                                  Y1="10"
                                                  Y2="0" />
                                    </Grid>
                                </Button>
                            </Grid>
                            <Grid Background="{TemplateBinding WindowContentBrush}" Grid.Row="1">
                                <Grid.RowDefinitions>
                                    <RowDefinition/>
                                    <RowDefinition Height="auto"/>
                                </Grid.RowDefinitions>
                                <TextBlock Text="{Binding Content,RelativeSource={RelativeSource Mode=TemplatedParent}}"
                                               VerticalAlignment="Center" HorizontalAlignment="Left" Margin="8" TextWrapping="Wrap" FontSize="14"/>
                                <Grid Grid.Row="1" Background="{TemplateBinding MessageBoxButtonBackground}">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Background="Transparent">
                                        <Button Style="{TemplateBinding OkButtonStyle}" x:Name="PART_OkButton" IsDefault="true">OK</Button>
                                        <Button Style="{TemplateBinding YesButtonStyle}" Visibility="Collapsed" x:Name="PART_YesButton">Yes</Button>
                                        <Button Style="{TemplateBinding NoButtonStyle}" Visibility="Collapsed" x:Name="PART_NoButton">No</Button>
                                        <Button Style="{TemplateBinding CancelButtonStyle}" Visibility="Collapsed" x:Name="PART_CancelButton">Cancel</Button>
                                    </StackPanel>
                                </Grid>
                            </Grid>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="MessageBoxButton" Value="OkCancel">
                            <Setter TargetName="PART_OkButton" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="PART_OkButton" Property="IsDefault" Value="false"/>
                            <Setter TargetName="PART_CancelButton" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="PART_CancelButton" Property="TabIndex" Value="1"/>
                            <Setter TargetName="PART_OkButton" Property="TabIndex" Value="2"/>
                        </Trigger>
                        <Trigger Property="MessageBoxButton" Value="YesNoCancel">
                            <Setter TargetName="PART_YesButton" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="PART_OkButton" Property="IsDefault" Value="false"/>
                            <Setter TargetName="PART_NoButton" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="PART_CancelButton" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="PART_OkButton" Property="Visibility" Value="Collapsed"/>
                            <Setter TargetName="PART_CancelButton" Property="TabIndex" Value="1"/>
                            <Setter TargetName="PART_NoButton" Property="TabIndex" Value="2"/>
                            <Setter TargetName="PART_YesButton" Property="TabIndex" Value="3"/>
                        </Trigger>
                        <Trigger Property="MessageBoxButton" Value="YesNo">
                            <Setter TargetName="PART_YesButton" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="PART_OkButton" Property="IsDefault" Value="false"/>
                            <Setter TargetName="PART_NoButton" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="PART_OkButton" Property="Visibility" Value="Collapsed"/>
                            <Setter TargetName="PART_NoButton" Property="TabIndex" Value="1"/>
                            <Setter TargetName="PART_YesButton" Property="TabIndex" Value="2"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>