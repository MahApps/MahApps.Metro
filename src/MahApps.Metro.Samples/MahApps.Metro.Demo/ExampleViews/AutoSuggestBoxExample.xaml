<UserControl x:Class="MetroDemo.ExampleViews.AutoSuggestBoxExample"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:MetroDemo"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DataContext="{d:DesignInstance local:MainWindowViewModel}"
             d:DesignHeight="600"
             d:DesignWidth="800"
             mc:Ignorable="d">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Grid.Column="0"
                   Grid.ColumnSpan="2"
                   Margin="0 0 0 10"
                   Style="{DynamicResource MahApps.Styles.TextBlock}"
                   Text="The box does not filter anything. It says that the text changed, and the code behind answers: the left one with artists whose name starts with what was typed, the right one with album titles that contain it. One suggests objects, the other plain strings."
                   TextWrapping="Wrap" />

        <mah:AutoSuggestBox x:Name="ArtistBox"
                            Grid.Row="1"
                            Grid.Column="0"
                            Margin="0 0 10 0"
                            mah:TextBoxHelper.ClearTextButton="True"
                            mah:TextBoxHelper.UseFloatingWatermark="True"
                            mah:TextBoxHelper.Watermark="Artist"
                            DisplayMemberPath="Name"
                            QuerySubmitted="ArtistBox_OnQuerySubmitted"
                            TextChanged="ArtistBox_OnTextChanged" />

        <mah:AutoSuggestBox x:Name="AlbumBox"
                            Grid.Row="1"
                            Grid.Column="1"
                            mah:TextBoxHelper.ClearTextButton="True"
                            mah:TextBoxHelper.Watermark="Album"
                            QuerySubmitted="AlbumBox_OnQuerySubmitted"
                            TextChanged="AlbumBox_OnTextChanged" />

        <TextBox x:Name="Log"
                 Grid.Row="2"
                 Grid.Column="0"
                 Grid.ColumnSpan="2"
                 Margin="0 10 0 0"
                 IsReadOnly="True"
                 Style="{DynamicResource MahApps.Styles.TextBox}"
                 TextWrapping="NoWrap"
                 VerticalScrollBarVisibility="Auto" />
    </Grid>
</UserControl>
